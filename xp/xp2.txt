Experiment - Sequential - Climatic

Data: ASOS
	country = ["fr","br"]
	variable = ["air_temperature","wind_direction","wind_speed","visibility","flights","delays","delay_perc","relative_humidity","apparent_temperature","pressure","dew_point"]
	n_years = [10] #365*24*10 = 87600
	
Methods: 
	corrtrack
	brute_force
	filcorr

ALWAYS FIXED parameters:
	
	PARALLEL_SKETCH = False
	PARALLEL_CANDIDATES = False
	PARALLEL_VALIDATION = False
	MAX_WORKERS = 0 (automatic = #CPUs)

	NEG_CORR = False
	TRAIN_RATIO = 0.5
	TARGET_RECALL = 0.9
	
	plot = True
	train_distances = False
	BASIC_WINDOW = None #automatic #BASIC_WINDOW approx sqrt(WINDOW_SIZE) (see: CorrTrack.get_bst_BASIC_WINDOW())
	RECALL_BY_WINDOW = True
	VERBOSE = False
	TESTING = False
	
	MONITOR = True
	TRACK_MIN_DIST = True
	CORR_VAL_OPTIM = False
	ARTIFACT_MODE = "final"
	

HYPERPARAMETER search:
	param_grid = {
		"n_vectors": [8,16,32,64],
		"cell_size": [0.25,0.5,0.75,1,1.25,1.5], #strech rate
		"freq_threshold": [0], #not needed in sequential

		"preprocess": [False],
		"nodes": [0], #automatic cores-1
		"seed": [2468],
		"seed_toggle": [1357],
		"grid_dimension": [1] #fixed
		"sketch_norm": ["mean_l2"] #fixed
		"candidate_backend": ["bptree"]
	}
	
		

====== XP 1 - scale n_series ======
DYNAMIC parameters:
	n_series = [5,25,50,75,100, max=128(climatic-fr)/143(climatic-br)/109({flights,delays,delay_perc}-br)]
	
FIXED parameters:
	WINDOW_SIZE = 7 * 24
	N_LAGS = WINDOW_SIZE*100
	WINDOW_STEP = BASIC_WINDOW
	CORR_THRESHOLD = 0.8
	CORR_VAL = True
	
====== XP 1.2 - scale n_series for all var. at the same time ======
DYNAMIC parameters:
	n_series = [5,25,50,75,100, max=all_X_all_var)]
	
FIXED parameters:
	WINDOW_SIZE = 7 * 24
	N_LAGS = WINDOW_SIZE*100
	WINDOW_STEP = BASIC_WINDOW
	CORR_THRESHOLD = 0.8
	CORR_VAL = True
	
====== XP 2 - scale WINDOW_STEP ======
DYNAMIC parameters:
	WINDOW_STEP = [1, 0.5*BASIC_WINDOW, BASIC_WINDOW]
	
FIXED parameters:
	n_series = 100
	WINDOW_SIZE = 7 * 24
	N_LAGS = WINDOW_SIZE*100
	CORR_THRESHOLD = 0.8
	CORR_VAL = True	
	
====== XP 3 - scale N_LAGS ======
DYNAMIC parameters:
	N_LAGS = WINDOW_SIZE*[1,10,100,1000]
	
FIXED parameters:
	n_series = 100
	WINDOW_SIZE = 7 * 24
	WINDOW_STEP = BASIC_WINDOW
	CORR_THRESHOLD = 0.8
	CORR_VAL = True

====== XP 4 - scale WINDOW_SIZE ======
DYNAMIC parameters:
	WINDOW_SIZE =  [7 * 24, 15 * 24, 30 * 24, 180 * 24, 365 * 24]
	
FIXED parameters:
	n_series = 100
	N_LAGS = WINDOW_SIZE*100
	WINDOW_STEP = BASIC_WINDOW
	CORR_THRESHOLD = 0.8
	CORR_VAL = True

====== XP 5 - scale CORR_THRESHOLD ======
DYNAMIC parameters:
	CORR_THRESHOLD = [0.7,0.8,0.9,0.95]
	
FIXED parameters:
	n_series = 100
	N_LAGS = WINDOW_SIZE*100
	WINDOW_STEP = BASIC_WINDOW
	WINDOW_SIZE = 7 * 24
	CORR_VAL = True	
	
====== XP 6 - CORR_VAL + NEG_CORR ======
DYNAMIC parameters:
	CORR_VAL = [True,False]
	NEG_CORR = [True,False]
	
FIXED parameters:
	n_series = 100
	N_LAGS = WINDOW_SIZE*100
	WINDOW_STEP = BASIC_WINDOW
	WINDOW_SIZE = 7 * 24
	CORR_THRESHOLD = 0.8